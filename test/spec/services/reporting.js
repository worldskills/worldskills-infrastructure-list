'use strict';

describe('Service: reporting', function () {

  // load the service's module
  beforeEach(module('ilApp'));

  // instantiate service
  var reporting;
  beforeEach(inject(function (_reporting_) {
    reporting = _reporting_;
  }));

  // it('should do something', function () {
  //   expect(!!reporting).toBe(true);
  // });

});
